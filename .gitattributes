# Keep line endings LF everywhere. Prettier is configured for LF, so a CRLF
# checkout on Windows would otherwise fail `npm run format:check`.
* text=auto eol=lf

# Binary assets must never be converted.
*.jpg binary
*.jpeg binary
*.png binary
*.gif binary
*.webp binary
*.ico binary
